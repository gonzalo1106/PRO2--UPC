using system;
using system.collection.generic;
using system.ling;
using system.text;
using system.threading.tasks;

namespace presentation1
{
    class program
    {
        static void main(string[] args) =>
    // Este es un  programa de  Hola mundo  
    /* rdtr rd un comrntsrio 
    de bloque */
    console.WriteLine("Hola mundo");
    console.ReadKey();
    

    }
}
